# TTS Loop

Speaks typed text on a loop while ducking (or pausing) other audio like Spotify,
releasing audio focus when stopped so playback resumes.

Push to GitHub → the Actions workflow builds `app-debug.apk` automatically.
